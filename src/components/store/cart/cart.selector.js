export const selectorCartIsCartOpen = (state)=>state.cart.isCartOpen
export const selectorCartItems = (state)=>state.cart.cartItems
export const selectorTotalPrice = (state)=>state.cart.totalPrice
export const selectorQuantity = (state)=>state.cart.quantity
